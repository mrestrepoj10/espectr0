# Dosquebradas seismic-microzonation research dossier

Status: **direct 5-zone table verified; municipal calculation and activation blocked**.

The official 2024 POT package publishes Table 27 with exact `To`, `Tc`, `TL`, `Aa`, `Fa`, and `Fv` values for five manually selected zones, and its figure states `β=5%`. The same current POT says the CARDER supports remain in force while ordering the municipality to adjust and harmonize the model with NSR-10 within 24 months.

No primary adopted formula, branch inclusivity, explicit `Av`, or return-period statement was found in the official package. This dossier therefore permits source-traceable display of the 30 direct cells only. It does not add an engine, UI, registry entry, map, coordinate lookup, GIS asset, or municipal calculation capability.

All three official PDFs remain external-only. Their exact URLs, byte lengths, page counts, SHA-256 hashes, page coordinates and extraction attestations are committed. No redistribution permission was inferred.

Run `node lib/dosquebradas/evidence/generate.mjs --check`, `python lib/dosquebradas/oracle/generate_oracle.py --check`, and `pnpm evidence:check`. `verify_official_sources.py` additionally re-checks locally supplied official bytes, hashes, page counts, table tokens and applicability clauses. Independent review and production activation both remain blocked.
